import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['res.cloudinary.com','canbind.ca','i.pravatar.cc','images.unsplash.com','www.mcentre.lk'],
  },
};

export default nextConfig;
