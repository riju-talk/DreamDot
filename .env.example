# DreamDot Environment Variables Example
# Copy this file to .env and fill in your actual values

# ===================================
# NEXT.JS WEB APP (apps/web/.env)
# ===================================

# Authentication & Security
NEXTAUTH_SECRET=generate_with_openssl_rand_base64_32
NEXTAUTH_URL=http://localhost:5000
JWT_SECRET=generate_with_openssl_rand_base64_32

# MongoDB
MONGO_CLUSTER=mongodb+srv://username:password@cluster.mongodb.net/dreamdot

# PostgreSQL Databases (5 separate databases)
POSTGRESS_DB_USER=postgresql://user:pass@host:5432/user_db
POSTGRESS_DB_SOCIAL=postgresql://user:pass@host:5432/social_db
POSTGRESS_DB_ITEMS=postgresql://user:pass@host:5432/items_db
POSTGRESS_DB_COMMUNITY=postgresql://user:pass@host:5432/community_db
POSTGRESS_DB_AUDIT=postgresql://user:pass@host:5432/audit_db

# ImageKit (Media Upload Service)
IMAGEKIT_PRIVATE_KEY=your_imagekit_private_key_here
NEXT_PUBLIC_IMAGEKIT_PUBLIC_KEY=your_imagekit_public_key_here
NEXT_PUBLIC_IMAGEKIT_URL_ENDPOINT=https://ik.imagekit.io/your_imagekit_id

# External Services URLs
NEXT_PUBLIC_CHAT_SERVER_URL=http://localhost:3001
NEXT_PUBLIC_PAYMENT_SERVER_URL=http://localhost:3002

# ===================================
# CHAT SERVER (apps/chat/.env)
# ===================================

# Server Configuration
PORT=3001
HOST=0.0.0.0
NODE_ENV=development

# Database
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/dreamdot

# Authentication (must match web app JWT_SECRET)
JWT_SECRET=same_as_web_app_jwt_secret

# CORS (comma-separated origins)
CORS_ORIGIN=http://localhost:5000,http://localhost:3000

# Socket.IO Configuration
SOCKET_PATH=/socket.io

# ===================================
# PAYMENT SERVICE (apps/payment/.env)
# ===================================

# Server Configuration
PORT=3002

# Stripe Payment Gateway
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Database
MONGO_CLUSTER=mongodb+srv://username:password@cluster.mongodb.net/dreamdot

# Authentication
JWT_SECRET=same_as_web_app_jwt_secret

# Service Integration
WEB_APP_URL=http://localhost:5000
SERVICE_SECRET=generate_random_secret_for_service_to_service_auth

# Client Configuration
CLIENT_URL=http://localhost:5000
CORS_ORIGIN=http://localhost:5000

# ===================================
# HOW TO GENERATE SECRETS
# ===================================

# NEXTAUTH_SECRET and JWT_SECRET:
# Run in terminal: openssl rand -base64 32

# Stripe Keys:
# Get from https://dashboard.stripe.com/test/apikeys

# Stripe Webhook Secret:
# Get from Stripe Dashboard > Developers > Webhooks
# Or use Stripe CLI: stripe listen --forward-to localhost:3002/webhook/stripe

# ImageKit Keys:
# Get from https://imagekit.io/dashboard/developer/api-keys

# MongoDB Connection String:
# Get from MongoDB Atlas > Clusters > Connect > Connect your application

# PostgreSQL Connection Strings:
# Format: postgresql://username:password@host:port/database
# Get from your PostgreSQL provider (Neon, Supabase, etc.)
